engine-strict=true
resolution-mode=highest

/*
Lisäselityksiä:

1. engine-strict=true
   - Tämä asetus varmistaa, että projekti käyttää tarkalleen määriteltyjä Node.js-versioita ja muita moottoreita (esim. npm-versio).
   - Jos asetus on `true`, npm estää pakettien asennuksen, jos Node.js:n tai npm:n versiot eivät täsmää projektin `package.json`-tiedostossa määriteltyjen versioiden kanssa.
   - Tämä auttaa varmistamaan, että projektin ympäristö on yhdenmukainen ja estää mahdolliset yhteensopivuusongelmat.

2. resolution-mode=highest
   - Tämä asetus määrittää, että npm valitsee korkeimman version riippuvuuksista, kun niitä ratkaistaan.
   - `highest`: Käyttää riippuvuuksista korkeinta saatavilla olevaa versiota, mikä voi olla hyödyllistä uusimpien ominaisuuksien ja korjausten saamiseksi.
   
   Nämä asetukset ovat hyödyllisiä projektin versionhallinnassa ja varmistavat, että kehitysympäristö on yhdenmukainen kaikkien kehittäjien kesken.
*/

